<header  id="top">
  <div class="social-main">
    <ul>
      <li><a class="sharetw" href="#"></a> </li>
      <li><a class="sharevk1" href="#"></a> </li>
      <li><a class="shareinst" href="#"></a> </li>
    </ul>
  </div>

    <div class="col-md-4 category">
        <% header_block_1.each do |t|  %>
          <div class="col-md-6 category_s"> <%= link_to t.category_name, category_path(t) %> </div>
        <% end %>
     </div>

    <div class="col-md-4 category"> <%= link_to image_tag("logo.png", alt:"Ely Blog"), root_path %> </div>

    <div class="col-md-4 category">
        <% header_block_2.each do |t|  %>
          <div class="col-md-6 category_s"> <%= link_to t.category_name, category_path(t) %> </div>
        <% end %>
     </div>


</header>

